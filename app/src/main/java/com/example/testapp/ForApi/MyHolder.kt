package com.example.testapp.ForApi

import android.view.View
import android.widget.TextView
import androidx.navigation.NavController
import androidx.navigation.Navigation
import androidx.recyclerview.widget.RecyclerView
import com.example.testapp.R

class MyHolder(itemView: View) : RecyclerView.ViewHolder(itemView) {
    /*var navc : NavController?= null , View.OnClickListener

    override fun onClick(view: View) {
        navc = Navigation.findNavController(view)
        navc?.navigate(R.id.action_filmsListFragment_to_filmsDetailsFragment)
    }*/
    /*private val textViewTitle:TextView = itemView.findViewById(R.id.textViewTitle)
    private val textViewVote:TextView = itemView.findViewById(R.id.textViewVote)
    private val textViewReleaseData:TextView = itemView.findViewById(R.id.textViewReleaseData)
    private val textViewOverview:TextView = itemView.findViewById(R.id.textViewOverview)


    fun bindView(result: Result){
        textViewTitle.text = result.original_title
        textViewVote.text = result.vote_average.toString()
        textViewReleaseData.text = result.release_date
        textViewOverview.text = result.overview
    }*/

}